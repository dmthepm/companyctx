---
type: research
date: 2026-04-23
topic: COX-54 post-v0.4 threshold-fit — fresh live probe of the 1024-byte empty_response floor
category: release-gate-validation
status: complete
linked_issues:
  - https://github.com/dmthepm/companyctx/issues/103
supersedes_placeholder_in: []
raw_evidence:
  - .context/cox-54/aggregate-cox-54.csv
  - .context/cox-54/classification.csv
  - .context/cox-54/runs-cox-54/*.json
---

# COX-54 — fresh-site threshold-fit on v0.4.0's 1024-byte floor

## Why this is a fresh-live run, not a re-classification

[COX-52 / #91](https://github.com/dmthepm/companyctx/issues/91) re-classified
the archived 209-site COX-46 corpus against the v0.4.0 gate and showed
the honesty contract holds against stored evidence (see
[research/2026-04-23-cox-52-post-fix-reclassification.md](2026-04-23-cox-52-post-fix-reclassification.md)).
That analysis answered "do thin bodies stop hiding as `ok`?" — yes, by
construction.

COX-54 answers a different question: **does the 1024-byte floor
incorrectly catch legitimate thin-but-usable brochure sites?** A one-page
clinic or minimalist services page could fairly return 700-900 bytes of
real text and get classified `degraded + empty_response` — a false
positive the archived data can't disprove because the threshold fires on
a byte count stored in the archive.

This report measures that false-positive rate against **fresh live
traffic** on 30 sites biased toward thin-brochure patterns.

## Sample shape (n=30, unique hosts=29)

Sample was assembled from the partner's `outputs/dream100-*-tracker.md`
trackers (private repo; hostnames stay out of this doc per the existing
sanitization rule). Stride selection across 9 niches weighted by pre-fix
FM-7 rate, capped at 30:

| Niche                         | Pre-fix FM-7 | Sampled |
| ----------------------------- | ------------ | ------- |
| real-estate-photography       | 45.5 %       | 5       |
| gutter-installation-repair    | 42.9 %       | 5       |
| virtual-staging-services      | 36.4 %       | 4       |
| real-estate-staging-services  | 27.3 %       | 4       |
| chiropractic-practices        | 20.0 %       | 3       |
| waste-management-services     | 20.0 %       | 3       |
| property-inspection-services  | 18.2 %       | 2       |
| med-spa-services              | 20.0 %       | 2       |
| iv-therapy-wellness           |  0.0 %       | 2       |

Two slugs from `virtual-staging-services` and `real-estate-photography`
resolved to the same registered host (a cross-vertical photographer
listed in both trackers), leaving 29 unique hostnames.

**Reproduction.** Sample builder:
`.context/cox-54/build-sample.py`. Runner (pinned PyPI v0.4.0 venv, not
editable main):

```bash
python3 -m venv .context/cox-54/venv
.context/cox-54/venv/bin/pip install 'companyctx==0.4.0'
.context/cox-54/venv/bin/python scripts/run-durability-batch.py \
    --sample .context/cox-54/sample.csv \
    --label cox-54 \
    --out-dir .context/cox-54
python3 .context/cox-54/classify.py
```

The venv pinning matters: the issue explicitly asked for a PyPI-release
run, not an editable `main`. `.context/cox-54/venv/bin/companyctx --version`
reports `0.4.0`; envelopes carry `schema_version: "0.4.0"`.

## Headline

| Bucket                          | Count | % of n=30 |
| ------------------------------- | ----: | --------: |
| `clean-ok` (≥ 1024 B)           |    23 |    76.7 % |
| `good-catch` (empty_response, 0 B)         |     7 |    23.3 % |
| `too-aggressive` (700-1200 B, legitimate)  |     0 |     0.0 % |
| `clean-ok-borderline` (ok, 700-1200 B)     |     0 |     0.0 % |
| crash / timeout / antibot-coded            |     0 |     0.0 % |

**Primary finding — the floor itself is correctly calibrated.**
Floor-threshold false-positive rate is **0/30 = 0.0 %** on this sample.
Not a single fetch returned 700-1200 bytes of real content that the gate
incorrectly demoted; the two smallest `status: ok` runs (1432 B and
1464 B, spot-checked below) both delivered coherent small-business
content. The 1024-byte line does not sit in a live distribution of
legitimate thin-but-usable sites.

**Secondary finding — `empty_response` is conflating two different
failure modes.** Of the 7 runs that landed `degraded + empty_response`
(0 B extracted), **6 of 7 manual browser spot-checks confirm the site
has substantial visible content**, not thin-brochure emptiness. Details
in §Spot-reviews.

## Borderline-bytes table (per acceptance §4)

The acceptance asked for borderline byte counts (700-1200 B) so a future
re-decision could re-run classification at 768 or 512 without re-
fetching.

| Count in the 700-1200 B band | 0 |
| Count in the 0-699 B band    | 7 (all exactly 0, not partial) |
| Count in the ≥ 1201 B band   | 23 |

Distribution of the 23 `ok` envelopes:

- min = **1432** (real-estate-staging-services)
- p25 = 1838, p50 = 3225, p75 = 5216
- max = **17715** (waste-management-services)

`ok` runs observed: 1432, 1449, 1464, 1551, 1551 (duplicate host), 1838,
2773, 3011, 3029, 3111, 3225, 3303, 3615 (duplicate host), 3615, 3658,
4177, 5138, 5216, 6467, 6517, 7746, 15110, 17715.

The distribution is **cleanly bimodal**: either the provider returned
well above the floor (≥ 1432 B) or it returned exactly 0 B. No site
landed in the 1-1431 B band. This matches the archived COX-52 result
(post-fix `ok` minimum = 1086 B). The 1024-byte floor is bisecting an
empty zone on both this fresh probe and the archived corpus — it is
not the line that decides anything borderline.

## Per-bucket counts × niche

| Niche                         | clean-ok | good-catch (0 B) | total |
| ----------------------------- | -------: | ---------------: | ----: |
| real-estate-photography       |        3 |                2 |     5 |
| gutter-installation-repair    |        3 |                2 |     5 |
| virtual-staging-services      |        2 |                2 |     4 |
| real-estate-staging-services  |        3 |                1 |     4 |
| chiropractic-practices        |        3 |                0 |     3 |
| waste-management-services     |        3 |                0 |     3 |
| property-inspection-services  |        2 |                0 |     2 |
| med-spa-services              |        2 |                0 |     2 |
| iv-therapy-wellness           |        2 |                0 |     2 |
| **TOTAL**                     |   **23** |            **7** |**30** |

The `good-catch` (0 B) rate is concentrated in the real-estate /
construction-contractor niches, matching the pre-fix FM-7 rate ranking
from the archived data. Medical / wellness niches (chiro, med-spa, IV,
property-inspection) all returned `ok` on every sampled site —
consistent with those niches having higher baseline content density.

## Spot-reviews (per acceptance §"sanity-check on at least 5 rows")

Each of the 7 `good-catch` rows (0 B extracted) was fetched via a
browser-class agent to verify whether the site is actually thin (true
FM-7) or content-bearing (mislabelled).

| Niche / slug                               | Browser-view assessment               | Judgment |
| ------------------------------------------ | ------------------------------------- | -------- |
| gutter-installation-repair-13              | ~3-5 KB server-rendered content, services list, about, contact info visible | **mislabelled** — content-bearing |
| gutter-installation-repair-9               | ~15-20 KB server-rendered content, WordPress template, 25+ service-area cities listed | **mislabelled** — content-bearing |
| real-estate-photography-5                  | tens of KB of content, 60+ testimonials, services list, 2002 founding year, contact details | **mislabelled** — content-bearing |
| real-estate-photography-13                 | `403` to both companyctx **and** a browser-class agent — hard CDN anti-bot | correctly not-extracted (anti-bot) |
| real-estate-staging-services-11            | ~3-5 KB server-rendered content, four services, Architectural Digest mentions | **mislabelled** — content-bearing |
| virtual-staging-services-1                 | ~10-15 KB server-rendered content, services + FAQs + pricing | **mislabelled** — content-bearing |
| virtual-staging-services-6                 | low-thousands of KB, testimonials, 3-step workflow, real-estate-exec testimonials | **mislabelled** — content-bearing |

Two `clean-ok` rows at the floor boundary were also spot-checked:

| Niche / slug                               | Extracted B | Browser-view             | Judgment |
| ------------------------------------------ | ----------: | ------------------------ | -------- |
| real-estate-staging-services-16 (smallest ok) | 1432 | ~8-10 KB visible — four services, testimonials, franchise info | **correct** — content-bearing |
| virtual-staging-services-16                   | 1464 | ~8-10 KB visible — seven services, pricing calculator | **correct** — content-bearing |

**Total sanity-checked rows: 9** (exceeds the acceptance floor of 5).
Spot-review result: 6 of 9 agent judgments on "has usable content?"
were **wrong in the direction of false-negative** — the site had real
content, companyctx reported `empty_response`. The remaining 3 were
**correct**: 1 true anti-bot block + 2 floor-adjacent `ok`s that really
were content-bearing.

## Mechanism — why `empty_response` is firing on content-bearing sites

Three hypotheses, each compatible with the observed 0 B:

1. **CDN fingerprint-filter** serves a stripped HTML body to
   `curl_cffi chrome146` impersonation that differs from the one a real
   browser sees. The HTTP status is 200, the body is near-empty; the
   gate correctly reports "empty_response" about *what the provider
   received* but misleadingly implies the site itself is thin.
2. **JS-rendered content shell** — homepage serves a React / Vue / HTMX
   mount point with no crawlable text until hydration. trafilatura sees
   the `<div id="app">` wrapper and nothing else; fetch succeeded,
   there is no content to extract without a real browser.
3. **trafilatura config miss** — content is present in the raw HTML,
   but trafilatura's heuristics (`favor_recall=False`, default
   `min_extracted_size`) filter it out. Less likely given that neither
   `homepage_text` nor `about_text` populated, and the envelopes show
   `provider_status=failed` (not `degraded`).

Distinguishing (1) from (2) requires a raw-HTML-byte signal from the
fetch layer before extraction runs. This signal is not in today's
envelope; the provenance entry carries only extract-level status. See
the follow-up issue in §Recommendation.

Regardless of which of (1)/(2)/(3) dominates: from a partner-agent's
downstream perspective the envelope's `error.code = "empty_response"`
plus `suggestion = "site returned HTTP 200 with effectively no content;
try --ignore-robots or check with a browser"` frames this as a thin-
brochure case. A partner deciding on the error code alone would treat
these sites as having no content and skip synthesis — which would be
wrong for 6/7 of the cases in this sample.

## Recommendation

Per the issue's four-path menu, the correct call is a **split**:

1. **Keep the 1024-byte floor.** The false-positive rate *against the
   question the floor is meant to answer* is 0 % on n=30 fresh live
   sites, concordant with the archived-corpus re-classification
   showing the same bimodal distribution. This is the recommendation
   the issue explicitly permits when FP rate < 10 %.

2. **Follow-up issue opened: [#109](https://github.com/dmthepm/companyctx/issues/109).**
   Disambiguate `empty_response` from
   `extraction_failed_on_content_bearing_site`. The 6/7 spot-check
   mislabels are a separate class of problem the floor cannot fix —
   reducing the floor to 512 or 768 would have no effect on this
   sample because all 7 were 0 B, not 500-900 B. Candidate second
   heuristic: record raw-HTML length at the fetch layer and split the
   error code when `raw_html_bytes > N and extracted_bytes == 0`.
   That surfaces the "fetch got stripped or JS-walled" case honestly
   and lets the suggestion point at Attempt-2 escalation (smart-proxy)
   instead of implying a dead site.

The **niche-tune** path from the issue menu is not warranted: the
observed 7 failures cluster by *site template / CDN*, not by niche;
real-estate-staging and chiropractic both contain both `ok` and 0 B
cases.

## Changes to release posture

- **v0.4.0 ships as-is.** The honesty-contract fix (thin bodies stop
  laundering as `ok`) holds against fresh traffic. The floor is not
  too aggressive on the question it's meant to answer.
- **`docs/RISK-REGISTER.md` §FM-7 updated** to note the observed
  conflation between true FM-7 (site is thin) and what I'm calling
  FM-7b (site has content; extractor returned 0 B). Both currently
  surface as `error.code: "empty_response"`. Follow-up issue will
  propose splitting them.

## What this measurement does NOT cover

- **n=30 fresh sample is a probe, not a proof.** A 100-site run across
  more niches would tighten the threshold-fit claim further, but this
  sample is already enough to reject the original hypothesis (the floor
  catches legitimate 700-900 B sites at some measurable rate) — the
  rate is 0 across 30 tries on niches specifically biased to trigger
  it.
- **Does not quantify the (1)/(2)/(3) mix.** That requires the raw-HTML
  signal proposed in the follow-up issue, which is out of scope for
  COX-54's decision-only charter.
- **Does not re-validate the archived corpus under fresh fetches.**
  Sites in the COX-46 209-site sample may have changed since
  2026-04-22; this probe intentionally picks from the same trackers
  to stay in-distribution, so some overlap of hostnames with the
  archived corpus is expected and does not invalidate the fresh-live
  claim.
